apiVersion: v1
kind: Service
metadata:
  name: {{ include "vote-app-chart.fullname" . }}-backend-svc
  namespace: {{ .Values.namespace.name }}
  labels:
    app: {{ include "vote-app-chart.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "vote-app-chart.fullname" . }}
    tier: backend
  ports:
  - name: http
    port: {{ .Values.service.port }}
    targetPort: 8000
